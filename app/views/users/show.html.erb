<% provide(:title, @user.name) %>
<div class="row">
    <asside class="span4">
        <section>
            <h1>
                <%= gravatar_for @user, size: 50 %>
                <%= @user.name %>
            </h1>
        </section>
    </asside>
</div>
